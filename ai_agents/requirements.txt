# ai_agents/requirements.txt
# ===========================
# Install with:  pip install -r ai_agents/requirements.txt
# Run Ollama separately (not pip-installable):
#   curl -fsSL https://ollama.com/install.sh | sh
#   ollama pull llama3.2-vision
#   ollama pull llama3(8GB)
#   ollama serve

# ── Core AI / LLM client ─────────────────────────────────────────────────────
ollama>=0.2.0              # Python client for local Ollama server

# ── Data validation ───────────────────────────────────────────────────────────
pydantic>=2.6.0            # Strict validation + model_validate_json

# ── Image processing ──────────────────────────────────────────────────────────
opencv-python-headless>=4.9.0  # Headless build — no GUI deps (safe for servers)
numpy>=1.24.0              # Required by OpenCV

# ── Utilities ────────────────────────────────────────────────────────────────
pillow>=10.0.0             # PIL used in the data pipeline
